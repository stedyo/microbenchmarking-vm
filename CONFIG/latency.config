# physical host IP [bridge interface]
TARGET_HOST_IP="10.0.0.1"

# targets vms IP (domainsU - Xen)
TARGET_DOMAIN_IP="domain1=10.0.0.10"

# overall time of the experiment
PING_COUNT=10

# interval that data will be display
INTERIM_DATA=0.5

#packet size (b)
PACKET_SIZE=100000